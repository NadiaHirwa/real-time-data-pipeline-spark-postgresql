[pytest]
# Registering markers here (rather than relying on pytest's implicit
# handling) means a typo like @pytest.mark.integrations is reported as
# an unknown-marker warning instead of silently matching nothing - which
# would quietly exclude a test from every -m selection.
markers =
    integration: requires a live PostgreSQL connection
